web.app.filebrowser.src.table.executors.Parade = function(web){
    this.withID = function(info){
        web.app.filebrowser.table.operates.parade.withID(info)
        web.app.filebrowser.table.operates.setting.ofEvent.withID(info)
    }
}