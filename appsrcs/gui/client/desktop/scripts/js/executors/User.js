web.app.gui.src.desktop.executors.User = function(web){
    this.check = function(){
        web.sys.adjusts.effect.ofProcess.toAppend('body','Loading')
        web.socket.data = new web.Data()
        web.socket.data.cmd = "web.app.gui.desktop.executes.user.check"
		web.socket.sends.data()
    }
}